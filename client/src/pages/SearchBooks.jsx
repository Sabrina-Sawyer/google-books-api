// Input field to search books and display results with Save buttons if logged in.

// const handleSearch = async (query) => {
//     const { data } = await client.query({
//       query: SEARCH_BOOKS,
//       variables: { query },
//     });
//     setSearchResults(data.searchBooks);
//   };